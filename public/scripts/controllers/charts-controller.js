var sampleApp = angular.module("azApp", ["firebase"]);
myApp.controller('ChartController',["$firebaseAuth","$http", function($firebaseAuth, $http) {
  var auth = $firebaseAuth();
  var self = this;
  self.amazonProperty = {};
  self.newSecret = {};
  self.results =[];
  self.amazonData=[];




















}]);
